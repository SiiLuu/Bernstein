apiVersion: v1
kind: Service
metadata:
  namespace: kube-public
  name: traefik
spec:
  selector:
    app: traefik
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      port: 30021
      targetPort: 30042
